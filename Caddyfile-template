https://{{WG_ENDPOINT}} {
    reverse_proxy /api/* api:9000 {
       root * /var/www/html/ 
        file_server
    }
}

https://{{GU_ENDPOINT}} {
    reverse_proxy / localhost:8080/guacamole {
        websocket
        transparent
    }
}
